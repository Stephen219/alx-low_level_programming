#include <stdio.h>
/**
 * main - the entry point of the project.
 * Return: 0 (Succes)
 * the method uses a for loop to get all the required numbers
 */
int main(void)
	int i;
{
	for (i = 0; 1 < 10 ; i++)
	{
		printf("%d", i);

	}
	putchar('\n');
	return (0);
}
