
#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strlen - this is the function that gets the lenth of the string
 * Return: int length of string if success
 * @s: the string paramete
 */

int _strlen(char *s)
{
	int size;

	size = strlen(s);
	return (size);
}
