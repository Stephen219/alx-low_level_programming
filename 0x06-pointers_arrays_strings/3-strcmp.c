#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *  _strcmp - this is the function thst compares the string.
 *  Return: int 0 incase it is true but -ve if false.
 *  @s1: first string
 *  @s2: string2
 */

int _strcmp(char *s1, char *s2)
{
	int ans = strcmp(s1, s2);

	return (ans);
}
